<Window x:Class="HR_ERP.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HR ERP — Sign In" Height="360" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Background="{StaticResource SurfaceBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BrandPrimary}" Padding="24,26">
            <StackPanel>
                <TextBlock x:Name="TitleText" Text="HR ERP" Foreground="White" FontSize="22" FontWeight="Bold"/>
                <TextBlock x:Name="SubtitleText" Text="Human Resources System" Foreground="#93A5B5" FontSize="12" Margin="0,3,0,0"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Margin="24,24,24,0">
            <TextBlock x:Name="UsernameLabel" Text="Username" FontWeight="SemiBold" Margin="0,0,0,2"/>
            <TextBox x:Name="UsernameBox" FontSize="14" Padding="8,7" KeyDown="Input_KeyDown"/>

            <TextBlock x:Name="PasswordLabel" Text="Password" FontWeight="SemiBold" Margin="0,10,0,2"/>
            <PasswordBox x:Name="PasswordBox" FontSize="14" Padding="8,7" KeyDown="Input_KeyDown"/>

            <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"
                       Margin="0,10,0,0" Visibility="Collapsed"/>

            <Button x:Name="SignInButton" Content="Sign In" Click="SignIn_Click"
                    Background="{StaticResource BrandAccent}" Foreground="White"
                    FontSize="14" Padding="10,9" Margin="0,16,0,0"/>
        </StackPanel>
    </Grid>
</Window>
