<Window x:Class="HR_ERP.Views.DocumentImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="280" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20" Background="{StaticResource SurfaceBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label x:Name="NameLabel" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox x:Name="NameBox" Width="240" Padding="6,5" VerticalContentAlignment="Center" KeyDown="NameBox_KeyDown"/>
            <TextBlock x:Name="ExtensionSuffixText" VerticalAlignment="Center" Margin="4,0,0,0" Foreground="Gray" FontSize="13"/>
        </StackPanel>

        <Label x:Name="CategoryLabel" Grid.Row="2" Margin="0,12,0,0"/>
        <!-- Tag holds the canonical English value (Important/Free/Other) that's stored and
             matched by the category filter; Content is the localized display text, set in
             ApplyLocalization. -->
        <ComboBox x:Name="CategoryBox" Grid.Row="3">
            <ComboBoxItem x:Name="CategoryImportantItem" Tag="Important"/>
            <ComboBoxItem x:Name="CategoryFreeItem" Tag="Free"/>
            <ComboBoxItem x:Name="CategoryOtherItem" Tag="Other"/>
        </ComboBox>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="OkButton" Click="Ok_Click" Background="{StaticResource BrandAccent}" Foreground="White" Padding="18,7" Margin="0,0,8,0"/>
            <Button x:Name="CancelButton" Click="Cancel_Click" Padding="18,7"/>
        </StackPanel>
    </Grid>
</Window>
